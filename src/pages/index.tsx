import React from 'react';

import { DefaultHeaderLayout, PlayerForm } from '../components';

const Index = () => (
  <DefaultHeaderLayout>
    <PlayerForm />
  </DefaultHeaderLayout>
);

export default Index;
